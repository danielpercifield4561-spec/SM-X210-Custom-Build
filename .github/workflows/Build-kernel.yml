name: Build Tab A9+ Standard GKI (No KSU)
on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Prepare Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git bc build-essential flex bison libssl-dev libelf-dev clang llvm lld gcc-aarch64-linux-gnu tar zip wget python3 cpio xz-utils dos2unix curl
          
      - name: 📥 Checkout Kernel Source
        uses: actions/checkout@v4

      - name: 🩹 Apply Deep Source Code Fixes
        run: |
          # 🛑 FIX: Strip 'q' from GKI (Force standard GKI identity)
          # This replaces 'qgki' with 'gki' in the config and Makefiles
          find . -type f -name "gta9pwifi_eur_open_defconfig" -exec sed -i 's/qgki/gki/g' {} + || true
          sed -i 's/qgki/gki/g' Makefile || true
          
          # 🛑 FIX: WALT TRACING Path
          sed -i 's|#define TRACE_INCLUDE_PATH .|#define TRACE_INCLUDE_PATH ../../kernel/sched/walt|g' kernel/sched/walt/trace.h || true
          
          # 🛑 FIX: ZSMALLOC Prototypes
          sed -i 's/void try_schedule_zs_compact()/void try_schedule_zs_compact(void)/g' mm/zsmalloc.c || true

          # 🛑 FIX: Samsung Display Syntax (Wildcard fix for all display files)
          find techpack/display -name "*.c" -exec sed -i 's/static inline _/static inline u32 _/g' {} + || true
          
          # 🛑 FIX: Module Bypass
          sed -i 's/return -ENOEXEC;/return 0;/g' kernel/module.c || true
          sed -i 's/same_magic = !strcmp(vermagic, modmagic);/same_magic = 1;/g' kernel/module.c || true

      - name: ⚙️ Kernel Config & Hardware Force-In
        run: |
          # Use the patched defconfig
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- gta9pwifi_eur_open_defconfig
          
          # Force WiFi and Sensors to be Built-in
          ./scripts/config --file .config --enable CONFIG_WLAN
          ./scripts/config --file .config --set-val CONFIG_QCACLD_WLAN y
          ./scripts/config --file .config --enable CONFIG_INPUT_SENSORHUB
          ./scripts/config --file .config --enable CONFIG_SENSORS_SSC
          
          # Identity
          ./scripts/config --file .config --set-str CONFIG_LOCALVERSION "-everything-works-gki"
          
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

      - name: 🔨 Compile
        run: |
          # Satisfy Samsung's version check string
          echo "-28972655-abX210XXS6CYB9" > .scmversion
          export LOCALVERSION=""
          FLAGS="ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- LLVM=1 LLVM_IAS=1 HOSTCC=gcc CC=clang -j$(nproc --all)"
          
          # Normal compiler flags without KSU definitions
          K_CFLAGS="-Wno-error -Wno-strict-prototypes -Wno-implicit-function-declaration"
          
          make $FLAGS Image KCFLAGS="$K_CFLAGS" 2>&1 | tee build.log

      - name: 📋 Build Summary
        if: always()
        run: |
          echo "::group::⚠️ VIEW ALL WARNINGS"
          grep -i "warning:" build.log || echo "No warnings found."
          echo "::endgroup::"
          echo "::group::❌ VIEW ALL ERRORS"
          grep -i "error:" build.log || echo "No errors found."
          echo "::endgroup::"

      - name: 📦 Package Output
        if: success()
        run: |
          if [ -f arch/arm64/boot/Image ]; then
            git clone https://github.com/osm0sis/AnyKernel3.git
            cp arch/arm64/boot/Image AnyKernel3/
            cd AnyKernel3 && zip -r9 ../TabA9Plus-GKI-Pure.zip *
          else
            exit 1
          fi

      - name: 📤 Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: TabA9Plus-GKI-Pure
          path: TabA9Plus-GKI-Pure.zip
